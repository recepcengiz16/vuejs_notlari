import { createApp } from 'vue' // bir değişken ya da method export ettiğimizde süslü parantez kullanırız.
import App from './App.vue'

//export const eventBus=new Vue();

createApp(App).mount('#app')
